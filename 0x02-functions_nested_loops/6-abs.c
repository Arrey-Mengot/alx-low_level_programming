#include "main.h"
#include <stdio.h>
/**
  *_abs - function to proint sign
  *@n: argument to function
  *Description: Function that prints the sign of a number
  *Return: 1 and + if positive, -1 and - if negative else 0
*/

int _abs(int n)
{
	if (n > 0)
	{
		return (n - 0);
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		return (0 - (-10));
	}
}
