#include "main.h"

/**
  *mul - function that multiplies two numbers
  *@a: first number(arguments)
  *@b: second number (arguments)
  *Description: This function takes two numbers and 
  *adds multiplies them
  *
 */

int mul(int a, int b)
{
	return (a * b);
}
